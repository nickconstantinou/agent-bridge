# Notes

The exact source edits for long prompt-containing documents should be applied from a local checkout or coding agent because this chat connector replaces complete files and may reject prompt-wrapper examples.
